general {
    lock_cmd = hyprlock
    before_sleep_cmd = hyprlock
}

listener {
    timeout = 600
    on-timeout = hyprlock
}

# DPMS listener commented out to prevent Hyprland crashes on AMDGPU Navi 10
# (where DPMS off triggers a monitor disconnect event, causing a segfault).
# listener {
#     timeout = 1200
#     on-timeout = hyprctl dispatch dpms off
#     on-resume = hyprctl dispatch dpms on
# }
